const initialState = {
    posts: {}
}

const postReducers = (state = initialState, action) => {
    switch (action.type) {
        default: {
            return state
        }
    }
}

export default postReducers;